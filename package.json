{
  "name": "oes-website-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix Backend",
    "prod": "npm run prod --prefix Backend",
    "install:backend": "npm install --prefix Backend"
  }
}